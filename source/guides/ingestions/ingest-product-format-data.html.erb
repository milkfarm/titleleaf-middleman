---
title: Ingest product format data
layout: guide_simple
parent: guide
category: ingestions
slug: ingest-product-format-data
---

<ol>
  <li>
    <p>
      Create a new Google Doc spreadsheet using the <em>product_formats</em>
      <%= templates_link 'template' %> (See menu item: File &gt; Make a Copy…).
      Name the new file as you deem necessary, but omit spaces
      (e.g., product_formats-YYYYMMDD where YYYYMMDD is today's date).
    </p>
    <p><%= cdn_image "google-file-menu-make-a-copy.png", alt: "Google Doc: File menu, Make a copy" %></p>
  </li>

  <li>
    <p>
      When the spreadsheet is complete
      (see <%= link_to "template notes", "/guides/ingestions/template-notes" %>),
      <a href="/guides/knowledge-base/download-google-doc-as-csv">download the data as a CSV file</a>
      to your local machine. Follow the same naming convention as in step 1,
      but add a ".csv" extension (e.g., products-YYYYMMDD.csv).
    </p>
  </li>

  <li>
    <p>Navigate to the new ingestion form.</p>
    <p><%= cdn_image "titleleaf-ingestion-menu.png", alt: "Ingestion menu" %></p>
  </li>

  <li>
    <p>Select <strong>Product Formats</strong> as the <a href="/guides/ingestions/template-to-ingestion-type-table">ingestion type</a>.</p>
  </li>

  <li>
    <p>Choose the local CSV file from step 2 to ingest.</p>
  </li>

  <li>
    <p>Click the "Create" button.</p>
  </li>
</ol>
